/**
 * Aura engine shared contracts.
 * Paste your full type definitions here (replace this placeholder).
 */

export type AuraEngineContractsPlaceholder = {
  /** Remove after pasting real contracts. */
  ready: false;
};
