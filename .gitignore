# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (may contain secrets)
.env
.env.local
.env.*
!.env.example

# Firebase service account credentials
serviceAccount.json

# Auto-generated FCM service worker config — publishable Firebase web keys;
# overwritten from VITE_FIREBASE_* at build when env vars are present.
# (Do not gitignore — Lovable deploy must serve this for firebase-messaging-sw.js.)
serviceAccount.json.json
*.json.json

# Question paper PDFs (local only — not committed)
public/papers/
